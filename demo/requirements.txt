# Hugging Face Space on ZeroGPU: the default (CUDA) torch wheel from PyPI.
# (For CPU hardware, add --extra-index-url https://download.pytorch.org/whl/cpu
# to get the much smaller CPU wheel.)
torch>=2.2
torchvision>=0.17
ultralytics>=8.3.0
lap>=0.5.12
av>=14.0
opencv-python-headless>=4.8
numpy>=1.24
scipy>=1.11
matplotlib>=3.7
gradio>=4.44
spaces            # ZeroGPU decorator; harmless on CPU hardware
